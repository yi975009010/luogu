#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        string s,res="";
        getline(cin,s);
        for(int j=0;j<s.size();j++){

        }
    }
    return 0;
}