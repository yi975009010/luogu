#include<bits/stdc++.h>
using namespace std;
const int N=1e4+10;
int a[N];

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n;
    cin>>n;
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    int i=1,j=2,max=0,ans=1;
    while(i<=n && j<=n){
        if(a[i]<)
    }

    return 0;
}
